import { dark } from 'mdx-deck/themes'
import future from '@mdx-deck/themes/future'
import highlight from '@mdx-deck/themes/syntax-highlighter-prism'

export const themes = [
  future,
  highlight,
  dark,
  {
    pre: {
      fontSize: '20px'
    },
  }
]
import { Appear } from 'mdx-deck'
import SiteSearch from '../src/molecules/site-search.js'
import NavBar from '../src/organisms/nav-bar.js'
import Button from '../src/atoms/button'
import Input from '../src/atoms/input'
import Label from '../src/atoms/label'
import NavLink from '../src/atoms/nav-link'
import NavLinks from '../src/molecules/nav-links'
import Underlay from './Underlay'
import { Split, FullScreenCode } from 'mdx-deck/layouts'

# Atomic Design
#### A Micro Project
###### By: Brent M Clark

---

# But First, About Me
I'm Brent M. Clark 👋

---

# Non-traditional background

- Worked in the restaurant industry for 10 years.
- Started in the tech industry as an IT generalist; moved into code shortly after.

--- 
# 13 years in the industry

I've worked for startups and fortune 500 companies alike.

--- 

# Front-end focused from the start.

- CSS1
- IE 5.5
- quirks mode
- Not even `jQuery` when I started
- Yuck

--- 

# What do I do now?
### I'm a gardener

I like to make potted plant arrangements, like this one.

<img src="https://keyassets.timeincuk.net/inspirewp/live/wp-content/uploads/sites/16/2011/09/pot5.jpg"/>

---

### Like any gardener, I searched for a sane way to manage my plants from seed to harvest.  So, I learned about seed starter pots.

<img src="https://gardenbetty-gardenbetty.netdna-ssl.com/wp-content/uploads/2011/03/2011-03-19-01-1080x810.jpg" width="800"/>

---

### After a few weeks the seeds have turned to seedlings.
But they are starting to outgrow their starter pots. They need a bigger home, so I move them to a larger pot.

<img src="https://gardenbetty-gardenbetty.netdna-ssl.com/wp-content/uploads/2019/02/2011-03-19-16.jpg" width="800"/>

---

A few more weeks goes by, and they are ready to plant in an arrangement.  :tada:

<img src="http://keyassets.timeincuk.net/inspirewp/live/wp-content/uploads/sites/16/2011/09/pot3.jpg" width="800"/>

---

### Ugh...  What does making plant arrangements have to do with my code?

---

### Our code goes through a very similar process when we make it.
- It starts small, needing only a small home
- It eventually grows more complex (bigger), needing a larger home
- We eventually need to mix the smaller pieces of code together to make something larger

---

# Atomic Design

---

# What's Atomic Design?

> "a methodology to craft interface design systems"

> -- Brad Frost

---

# Well that's not very helpful.

<Appear>
  <p>
  In my own words, Atomic Design is an approach to building small pieces of software
  <br />
  that come together to make bigger pieces of software.
  </p>
</Appear>

---

## Atomic Design Borrows From Chemistry
by using names that we can all understand.

<img src="http://atomicdesign.bradfrost.com/images/content/atomic-design-process.png" width="800" />

---

# Why should <em>I</em> use Atomic Design?
#### Read: What's in it for me?

---

### Atomic Design is:

<ul>
  <Appear>
    <li>DRY <a href="https://en.wikipedia.org/wiki/Don%27t_repeat_yourself">link </a></li>
    <li>Easy to test</li>
    <li>Easy to modify</li>
    <li>Safe to modify</li>
  </Appear>
</ul>

--- 

🤔 OK cool.  How do I use Atomic Design?

---

Making a `NavBar` component, the atomic way.
<Underlay>
    <NavBar />
</Underlay>

# ☝️The goal.

---

### Let's start with the `Atoms`

---

### The `Button` Atom
<Underlay>
  <Button>Search</Button>
</Underlay>

---

### The Code
```jsx
const Button = styled.button`
    background: #28AFB0;
    color: white;
    text-transform: uppercase;
    border: 0;
    font-weight: 700;
    padding: 8px 8px;
`
```

---

### The `NavLink` Atom

<Underlay>
  <NavLinks>
    <NavLink>Home</NavLink>
  </NavLinks>
</Underlay>

---

### The Code

```jsx
const Wrapper = styled.li`
    margin: 0 8px;

    a {
        color: #28AFB0 !important;
        font-size: 10px;
        text-decoration: none;
        font-weight: bold;

        :hover {
            text-decoration: underline;
        }
    }
`

function NavLink(props) {
    const { children, className, ...rest} = props
    return (
        <Wrapper className={className}>
            <a {...rest}>{children}</a>
        </Wrapper>
    )
}
```

---

### The `Label` Atom
<Underlay>
  <Label>Search The Site</Label>
</Underlay>

---

### The Code
```jsx
const Label = styled.label`
  text-transform: uppercase;
  color: black;
  font-size: 10px;
`
```

---

### The `Input` Atom
<Underlay>
  <Input type="text" placeholder="Enter Keyword" />
</Underlay>

---

### The Code
```jsx
const Input = styled.input`
    text-transform: uppercase;
    background: #37392E;
    color: white;
    padding: 8px 12px;
    border: 0;
    font-weight: 700;
`
```

---

### Now let's check out the `Molecules`

---

### The `NavLinks` Molecule

<Underlay>
  <NavLinks>
    <NavLink>Home</NavLink>
    <NavLink>Blog</NavLink>
    <NavLink>About</NavLink>
  </NavLinks>
</Underlay>

---

### The Code

```jsx
const NavLinks = styled.ul`
  display: flex;
  list-style-type: none;
  margin: 0 80px 0 0;
  padding: 0;
`
```

---

# The `SiteSearch` Molecule
<br />
<Underlay>
  <SiteSearch />
</Underlay>

---

<FullScreenCode>

``` jsx
import Button from '../atoms/button'
import Label from '../atoms/label'
import Input from '../atoms/input'

function SiteSearch(props) {
    return (
        <Form
            onSubmit={event => {
                event.preventDefault();
                const searchTerm = event.target.elements[0].value
                alert('Searching for' + searchTerm)
            }}
        >
            <Label>Search The Site</Label>
            <div>
                <Input type="text" placeholder="Enter Keyword" />
                <Buttontype="submit">Search</Button>
            </div>
        </Form>
    )
}

const Form = styled.form`
    text-align: left;

    label {
        margin: 0 0 4px;
        display: inline-block;
    }

    input {
        margin: 0 4px 0 0;
    }
`

```
</FullScreenCode>

---

### Lastly, let's have a look at our `Organisms`

---

###  There's really only one organism; `NavBar`

<Underlay>
    <NavBar />
</Underlay>

---

<FullScreenCode>

```jsx
import NavLink from '../atoms/nav-link'
import NavLinks from '../molecules/nav-links'
import SiteSearch from '../molecules/site-search'

function NavBar(props) {
    const [showSearch, setShowSearch] = useState(false);
    return (
        <Container>
            <NavLinks>
                <NavLink href="#Home">Home</NavLink>
                <NavLink href="#Blog">Blog</NavLink>
                <NavLink href="#About">About</NavLink>
            </NavLinks>
            <SearchIcon onClick={() => setShowSearch(!showSearch)} role="img" aria-label="search">🔍</SearchIcon>
            {
                showSearch &&
                <SearchWrapper>
                    <SiteSearch />
                </SearchWrapper>
            }
        </Container>
    )
}

const Container = styled.nav`
    background: #fafafa;
    padding: 8px 24px;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-flow: row nowrap;
    border: 1px solid #ccc;
    position: relative;
`

const SearchWrapper = styled.div`
    position: absolute;
    top: calc(100% - 10px);
    right: 0;
    border: 1px solid #CCC;
    background: #FAFAFA;
    padding: 20px;
    border-radius: 6px;
`

const SearchIcon = styled.span`
    cursor: pointer;
`
```

</FullScreenCode>

---

# Questions?