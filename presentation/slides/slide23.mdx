import Layout from './Layout'
import Underlay from './Underlay'
import NavLink from '../../src/atoms/nav-link.js'
import NavLinks from '../../src/molecules/nav-links.js'
import { Notes } from 'mdx-deck'

<Layout>

### The `NavLink` Atom

<Underlay>
  <NavLinks>
    <NavLink>Home</NavLink>
  </NavLinks>
</Underlay>

</Layout>

<Notes>
The NavLink is blue, kinda boring.  But under the hood there's a different story.  It's a nav link, which are typically anchor elements wrapped in a list item.
</Notes>